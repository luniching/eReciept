var path = require('path'),
	term = require('../term'),
	cliUtil = require('../util');

module.exports = {
	generate: function() {
		term.exit(new Error('Not implemented: generator \'test\''));
	}
};
